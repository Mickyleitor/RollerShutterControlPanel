name: build

on: [push, pull_request]

jobs:
  build:
    strategy:
      matrix:
        project_dir: ["Firmware_Arduino", "Firmware_ESP8266"]
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Cache PlatformIO
        uses: actions/cache@v4
        with:
          path: ~/.platformio
          key: pio-${{ runner.os }}-${{ hashFiles(format('{0}/platformio.ini', matrix.project_dir)) }}
          restore-keys: |
            pio-${{ runner.os }}-

      - name: Install PlatformIO Core
        run: pip install -U platformio

      # Compila todos los entornos definidos en platformio.ini; usa -e si matrices de env
      - name: Build ${{ matrix.project_dir }}
        working-directory: ${{ matrix.project_dir }}
        run: |
          pio run
        # Si defines matriz de entornos: pio run -e ${{ matrix.env }}

      # Publica los artefactos generados; hex para AVR; bin para ESP8266
      - name: Upload artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.project_dir }}-artifacts
          path: |
            ${{ matrix.project_dir }}/.pio/build/*/*.hex
            ${{ matrix.project_dir }}/.pio/build/*/*.bin
            ${{ matrix.project_dir }}/.pio/build/*/*.elf
